<?xml version="1.0" encoding="UTF-8" ?>
<Application xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:NotifyGen.MauiSample"
             x:Class="NotifyGen.MauiSample.App">
  <Application.Resources>
    <ResourceDictionary>
      <Color x:Key="Accent">#00D4FF</Color>
      <Color x:Key="PageBg">#0C1220</Color>
      <Color x:Key="Panel">#162036</Color>
      <Color x:Key="Text">#E6ECF5</Color>
      <Style TargetType="ContentPage">
        <Setter Property="BackgroundColor" Value="{StaticResource PageBg}" />
      </Style>
      <Style TargetType="Label">
        <Setter Property="TextColor" Value="{StaticResource Text}" />
      </Style>
      <Style TargetType="Entry">
        <Setter Property="TextColor" Value="{StaticResource Text}" />
        <Setter Property="BackgroundColor" Value="{StaticResource Panel}" />
      </Style>
      <Style TargetType="Button">
        <Setter Property="BackgroundColor" Value="{StaticResource Accent}" />
        <Setter Property="TextColor" Value="#0C1220" />
      </Style>
    </ResourceDictionary>
  </Application.Resources>
</Application>
