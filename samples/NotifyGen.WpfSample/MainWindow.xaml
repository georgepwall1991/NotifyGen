<Window x:Class="NotifyGen.WpfSample.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:NotifyGen.WpfSample"
        Title="NotifyGen WPF Sample" Height="450" Width="600">
    <Window.DataContext>
        <local:MainViewModel />
    </Window.DataContext>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Grid.ColumnSpan="2"
                   Text="NotifyGen Demo - INotifyPropertyChanged Made Easy"
                   FontSize="18" FontWeight="Bold" Margin="0,0,0,20"/>

        <!-- First Name -->
        <TextBlock Grid.Row="1" Grid.Column="0" Text="First Name:" VerticalAlignment="Center"/>
        <TextBox Grid.Row="1" Grid.Column="1"
                 Text="{Binding FirstName, UpdateSourceTrigger=PropertyChanged}"
                 Margin="0,5"/>

        <!-- Last Name -->
        <TextBlock Grid.Row="2" Grid.Column="0" Text="Last Name:" VerticalAlignment="Center"/>
        <TextBox Grid.Row="2" Grid.Column="1"
                 Text="{Binding LastName, UpdateSourceTrigger=PropertyChanged}"
                 Margin="0,5"/>

        <!-- Full Name (computed, uses NotifyAlso) -->
        <TextBlock Grid.Row="3" Grid.Column="0" Text="Full Name:" VerticalAlignment="Center" FontWeight="Bold"/>
        <TextBlock Grid.Row="3" Grid.Column="1"
                   Text="{Binding FullName}"
                   VerticalAlignment="Center" Margin="0,5"
                   Foreground="DarkGreen" FontWeight="Bold"/>

        <!-- Age with slider -->
        <TextBlock Grid.Row="4" Grid.Column="0" Text="Age:" VerticalAlignment="Center"/>
        <StackPanel Grid.Row="4" Grid.Column="1" Orientation="Horizontal" Margin="0,5">
            <Slider Value="{Binding Age}" Minimum="0" Maximum="120" Width="200"/>
            <TextBlock Text="{Binding Age}" Margin="10,0" VerticalAlignment="Center"/>
        </StackPanel>

        <!-- Status Log -->
        <GroupBox Grid.Row="5" Grid.ColumnSpan="2" Header="Property Change Log" Margin="0,20,0,0">
            <ScrollViewer>
                <TextBlock Text="{Binding StatusLog}" TextWrapping="Wrap" FontFamily="Consolas"/>
            </ScrollViewer>
        </GroupBox>
    </Grid>
</Window>
