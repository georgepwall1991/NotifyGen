# GitHub discoverability checklist

Manual steps (token scopes for cloud agents may block repository metadata writes):

1. Replace topic typo `source-genertator` with `source-generator`.
2. Keep topics: `csharp`, `dotnet`, `mvvm`, `inotifypropertychanged`, `source-generators`, `roslyn`, `wpf`, `maui`, `avalonia`, `blazor`, `nuget`.
3. Enable Discussions and seed a “Migration / Show and tell” category pointing at `docs/migrate-from-communitytoolkit.md`.
4. Pin the 2.0 launch post or migration doc in the repo About / README docs links (already linked from README).
